OPEN TERMINAL

type these in order:
python3 -m ensurepip --upgrade
pip install flask

ifconfig <-----------------(FIND IPV4: http://127.0.0.1:5000 )
                                                         ^this port is changeable between 5000 and 8000 in app.py
python3 --version

IF NOT INSTALLED THEN: 
Open Safari (or any web browser).
Go to https://www.python.org/downloads/.
Click the big yellow Download Python 3.x.x button.
After it downloads, open the file (you’ll find it in your Downloads folder).
Follow the instructions to install Python.


type this in TERMINAL to start the server:
python3 app.py

type this in SAFARI to access app:
default should be : http://127.0.0.1:5000
else: http://uripv4:5000 or 8000
PRESS CMD C TO STOP SERVER 
